import string
import random

attributes = string.ascii_uppercase +string. ascii_lowercase+ string.digits + string.punctuation

print('Enter the length of password required')

length = int(input())

password = ''.join(random.choices(attributes, k=length))

print("Your required password is:",)

input(password)
